<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Folder</title>
    <link href="/stylesheets/output.css" rel="stylesheet" />
    <link rel="icon" href="/Images/icon.svg" />
  </head>
  <body
    class="bg-gray-100 font-sans flex items-center justify-center min-h-screen"
  >
    <div class="container mx-auto p-4 max-w-sm">
      <h1 class="text-2xl font-bold text-center mb-6">Create a New Folder</h1>

      <% if (errors && errors.length > 0) { %>
      <ul
        class="mb-4 bg-red-100 text-red-700 border border-red-400 rounded p-3"
      >
        <% errors.forEach(error => { %>
        <li><%= error.msg %></li>
        <% }) %>
      </ul>
      <% } %>

      <form
        action="/files/folders/create"
        method="post"
        class="bg-white shadow-md rounded p-6"
      >
        <div class="mb-4">
          <label for="name" class="block text-sm font-medium text-gray-700"
            >Folder Name:</label
          >
          <input
            type="text"
            id="name"
            name="name"
            value="<%= name %>"
            required
            class="mt-1 block w-full border border-gray-300 rounded-md p-2 focus:ring focus:ring-blue-500"
          />
        </div>
        <button
          type="submit"
          class="w-full bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 rounded transition"
        >
          Create Folder
        </button>
      </form>
    </div>
  </body>
</html>
