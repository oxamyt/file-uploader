<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Folders Page</title>
    <link href="/stylesheets/output.css" rel="stylesheet" />
  </head>
  <body class="bg-gray-100 font-sans flex flex-col items-center min-h-screen">
    <div class="container mx-auto p-4 max-w-2xl">
      <h1 class="text-3xl font-bold text-center mb-6">Your Folders</h1>

      <% if (folders.length > 0) { %>
      <ul class="space-y-4">
        <% folders.forEach(folder => { %>
        <li
          class="bg-white shadow-md rounded p-4 flex flex-col md:flex-row justify-between items-start md:items-center"
        >
          <div class="mb-2 md:mb-0">
            <h2 class="text-xl font-semibold"><%= folder.name %></h2>
          </div>
          <div class="flex space-x-2">
            <a
              href="/files/folders/update/<%= folder.id %>"
              class="bg-blue-500 text-white font-bold py-2 px-3 rounded hover:bg-blue-600 transition text-sm"
              >Update</a
            >
            <a
              href="/files/folders/<%= folder.id %>"
              class="bg-blue-500 text-white font-bold py-2 px-3 rounded hover:bg-blue-600 transition text-sm"
              >View Files</a
            >
            <form action="/files/folders/delete/<%= folder.id %>" method="post">
              <button
                type="submit"
                class="bg-red-500 text-white font-bold py-2 px-3 rounded hover:bg-red-600 transition text-sm"
              >
                Delete
              </button>
            </form>
          </div>
        </li>
        <% }) %>
      </ul>
      <% } else { %>
      <p class="text-gray-700 text-center">You don't have any folders yet.</p>
      <% } %>

      <div
        class="mt-6 flex flex-col md:flex-row justify-center space-y-2 md:space-y-0 md:space-x-4"
      >
        <a
          href="/files/folders/create"
          class="bg-blue-500 text-white font-bold py-2 px-4 rounded hover:bg-blue-600 transition"
          >Create a New Folder</a
        >
        <a
          href="/files/upload"
          class="bg-blue-500 text-white font-bold py-2 px-4 rounded hover:bg-blue-600 transition"
          >Upload File</a
        >
        <a
          href="/share-folder"
          class="bg-blue-500 text-white font-bold py-2 px-4 rounded hover:bg-blue-600 transition"
          >Share Folder</a
        >
      </div>
    </div>
  </body>
</html>
